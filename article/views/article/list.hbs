<!DOCTYPE html>
<html lang="en">

<head>
  <title>{{title}}</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/static/bootstrap/css/bootstrap.min.css">
  <link rel="stylesheet" href="/static/css/style.css">
</head>

<body>
  <div class="jumbotron">
    <div class="container">
      <h1>文章列表</h1>
    </div>
  </div>
  <div class="container">
    <div class="row bar">
      <div class="col-md-8">
        <div class="btn-group" role="group" aria-label="...">
          <a class="btn btn-primary" href="/article/add">发布文章</a>
        </div>
      </div>
      <div class="col-md-4">
        <form class="input-group" action="/article/search">
          <input type="text" class="form-control" name="keyword" placeholder="输入关键字" value="{{keyword}}">
          <span class="input-group-btn">
            <button class="btn btn-success" type="submit">搜索</button>
          </span>
        </form>
      </div>
    </div>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th width="10%">ID</th>
          <th>标题</th>
          <th width="15%">发布时间</th>
          <th width="15%">更新时间</th>
          <th width="15%">操作</th>
        </tr>
      </thead>
      <tbody>
        {{#if list}}
        {{#each list}}
        <tr>
          <td>{{id}}</td>
          <td><a href="/article/{{id}}">{{title}}</a></td>
          <td>{{datetime created_at}}</td>
          <td>
            {{#if updated_at}}
              {{datetime updated_at}}
            {{else}}
              -
            {{/if}}
          </td>
          <td>
            <div class="btn-group btn-group-sm" role="group">
              <a class="btn btn-warning" href="/article/edit/{{id}}">修改</a>
              <a class="btn btn-danger" href="/article/delete/{{id}}">删除</a>
            </div>
          </td>
        </tr>
        {{/each}}
        {{else}}
          <tr>
            <td colspan="5" style="text-align: center;">
              {{#if keyword}}
              暂无与 {{keyword}} 相关的文章  
              {{else}}
              还没有发布文章
              {{/if}}
            </td>
          </tr>
        {{/if}}
      </tbody>
    </table>
  </div>
</body>

</html>
